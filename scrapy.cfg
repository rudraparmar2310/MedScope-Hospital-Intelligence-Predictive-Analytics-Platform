# Scrapy project marker file.
#
# Its presence at the repository root is what makes `scrapy crawl hospitals`
# and `scrapy list` work from the command line; the ingestion pipeline itself
# runs the same spiders programmatically through src/ingestion/scraper/runner.py.
[settings]
default = src.ingestion.scraper.settings

[deploy]
project = medscope
