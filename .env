DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medscope_db
API_URL=http://localhost:8000
MODEL_PATH=models/model.pkl
PORT=8000
HOST=127.0.0.1

# External data ingestion pipeline
# Required for the POST /ingestion/run endpoint (Ingestion Monitor -> Trigger run).
# Callers must send the same value in the X-Ingestion-Key header.
INGESTION_API_KEY=medscope123
